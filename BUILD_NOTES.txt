# ACKuifer Build Notes & Open Items
# Updated: Session 6 (Source Discovery Data Quality)
# Keep this file in your project root and update as you go.

## KNOWN DATA ISSUES
- Docs 260081 and 303051 had street_name=None due to Laserfiche viewer UI chrome
  contaminating the address field. Parser fix applied in Step 4. Street names
  manually corrected to "Beacon Lane" and "Quidnet Road" respectively.
- 171 Source Discovery records have geocode_review_needed=True and no lat/lng.
  All excluded from map display until geocoded. Addressed in Step 5.

## SESSION 6 CHANGES (Source Discovery Data Quality)
- MapGeo geocoder (nantucket.mapgeo.io) broken — returning 404 as of 2026-03-17.
  Replaced both _address_to_latlong() calls in _geocode_location() with
  _nominatim_geocode(). _address_to_latlong() retained but marked deprecated.
  _nominatim_geocode() now checks for existing "nantucket" in address to avoid
  double-appending ", Nantucket, MA".
- KNOWN_WELL_COORDS in massdep.py replaced with verified Google Maps parcel
  centroids. Previous values were fabricated during Step 5 build and plotted in
  Shimmo. All VDT wells now correctly placed: 4FG→4 Fairgrounds Rd,
  2FG→2 Fairgrounds Rd, 6FG→6 Fairgrounds Rd, OS→Old South Rd, WAITT→Waitt Dr,
  TIC→Ticcoma Way.
- 3 ghost records deleted: id=96 (truncated sample_location, CAS numbers mistaken
  for concentrations), id=115 and id=151 (QC page parsing artifacts — Lab
  Duplicate and Matrix Spike pages).
- massdep.py parser updated to skip QC pages (Lab Duplicate Analysis, Matrix
  Spike Analysis, Lab Control Sample, Method Blank) during compound extraction.
- 43 of 55 drinking_water geocode-review records resolved via suffix stripping
  (_INF, _EFF, -R-3, -3, -M-3, -C-3, _2, -F) + Nominatim. 12 remain
  (8 unresolved streets, 4 FRB wells).
- clean_address column added to source_discovery_results for admin geocode queue
  display.
- ALERT LOGIC DECISION: drinking_water Source Discovery records alert same as
  Laserfiche results. groundwater records (VDT monitoring wells) display on map
  only, no alerts. soil records filtered from display entirely.
- NOTE: if a drinking_water Source Discovery result and a Laserfiche result exist
  for the same address, dedup alert logic needed to prevent double-alerting.
  Handle in notification engine.
- EEA portal has newer uningested PDFs: "Lab Data. PFAS. June-August 2025.pdf",
  "Nantucket Data 04-09-25 Fairgrounds.pdf", "Nantucket Data 04-09-25
  Landfill.pdf". Run massdep scraper to ingest before next map review.
- Branch claude/objective-hoover ready to PR to main once geocode review is
  complete.

## KNOWN_WELL_COORDS COORDINATE FIX (Session 5, 2026-03-17)
- KNOWN_WELL_COORDS in massdep.py had fabricated coordinates that plotted in
  Shimmo (~41.28, -70.06) instead of the Fairgrounds area mid-island.
- Replaced with verified Google Maps parcel centroids:
  - VDT-4FG wells → 41.267468, -70.087756 (4 Fairgrounds Road)
  - VDT-2FG wells → 41.268764, -70.086947 (2 Fairgrounds Road)
  - VDT-OS wells  → 41.267879, -70.084221 (Old South Road)
  - VDT-WAITT     → 41.266763, -70.086734 (Waitt Drive)
  - VDT-TIC       → 41.266588, -70.088298 (Ticcoma Way)
- Updated 13 groundwater records in source_discovery_results with corrected
  lat/lng and re-assigned neighborhoods (Mid-Island, Monomoy).
- Removed fabricated VDT-6FG-7/8/9/11 and VDT-OSR-1/2/3 entries that had no
  matching database records. Added VDT-4FG-1/2/3, VDT-2FG-7/9, VDT-OS-8,
  VDT-TIC-11 entries that exist in the database.
- VDT-6FG-10 → 41.267417, -70.088085 (6 Fairgrounds Road) — added and DB updated.
- PROJECT_CENTROID updated to 41.2675, -70.0878 (Fairgrounds area).
- All VDT- well coordinates in KNOWN_WELL_COORDS are now verified against
  Google Maps parcel centroids. MW-1/2/3 (Airport Road) not yet verified.

## QC PAGE PARSING FIX & DATA CLEANUP (Session 5, 2026-03-17)
- Deleted 3 ghost records parsed from QC pages in Pace lab PDFs:
  - id=96: VDT-TIC-11 [0.5- (groundwater) — 3538 ppt HAZARD — compound values
    were CAS registry numbers, not concentrations. Truncated sample_location.
  - id=115: "4" (groundwater) — 399.89 ppt — Lab Duplicate Analysis page for
    4 TOMS WAY-3; Client ID wrapped across lines, parser captured only "4".
  - id=151: "20 TOMS" (groundwater) — 83.15 ppt — Matrix Spike Analysis page for
    20 TOMS WAY-3; Client ID wrapped, parser captured "20 TOMS".
- Parser fix: compound extraction loop now skips pages containing QC headers
  (Lab Duplicate Analysis, Matrix Spike Analysis, Lab Control Sample Analysis,
  Method Blank Analysis) to prevent future ghost records.
- Fixed VDT-TIC-11 and VDT-OS-8 soil record coordinates (were still using old
  Shimmo values from before the coordinate fix).

## SOURCE DISCOVERY DATA INTEGRITY FIXES (Session 5, 2026-03-17)
Three issues resolved in source_discovery_results:

### 1. Schema: added depth and medium columns
- Added `depth` (text, nullable) and `medium` (text, nullable) to
  source_discovery_results table and SourceDiscoveryResult model.
- Backfilled from existing sample_location strings:
  - medium extracted from final parenthesized group (e.g. "(groundwater)")
  - depth extracted from bracket/paren depth notation (e.g. "[0.5-0.75']")
- Result: 94 drinking_water, 45 groundwater, 32 soil. All 32 soil records
  have depth populated.

### 2. Soil records excluded at query layer
- Soil records retained in DB but excluded from map API (api.py) and
  notification engine (engine.py) via `medium != 'soil'` filter.
- Admin geocode review queue intentionally unfiltered (operator sees all).
- 32 soil records are low-value (max PFAS6 = 1.25 ppt) but preserved for
  data completeness.

### 3. Cross-PDF duplicate merge and dedup fix
- Identified 15 duplicate groups (same sample from different MassDEP PDFs).
  Each PDF reported different compound subsets, so records had different
  PFAS6 sums.
- Merged compound values across duplicates: for each of 6 compounds, took
  max non-null value. Recalculated pfas6_sum and result_status from merged
  compounds using classify_result_status().
- Updated 15 canonical records, deleted 27 duplicate records.
  144 total records remain (was 171).
- Notable: VDT-4FG-4 remains HIGH-DETECT at 22.33 ppt (merged 5 compounds
  including PFHpA=8.62 from a secondary PDF).
- Updated massdep.py _save_location() dedup logic:
  - OLD: dedup by source_doc_url (same well from same PDF only)
  - NEW: dedup by composite key (sample_location, sample_date, medium)
  - When a match is found, merges compound values instead of skipping
- Dropped unique constraint on source_doc_url column (no longer a dedup key).
- seen_documents PDF-level dedup unchanged (still prevents re-downloading).

## SCHEMA / DATABASE NOTES
- Error queue is implemented as seen_documents.parse_status='error' (not a
  separate error_queue table). Admin interface (Step 8) must query
  seen_documents WHERE parse_status='error' for the parse error queue display.
- seen_documents uses doc_key + source columns to handle both Laserfiche doc IDs
  and EEA PDF URLs in one table.

## PARSER NOTES
- Boundary bug in prototype: test_report_parser.py and pace_lab_parser.py both
  use <= 89.9 instead of < 90.0 for HIGH-DETECT. Fixed in production via
  classify_result_status() in config.py. Do not reuse prototype status logic.
- pace_lab_parser.py has PFHpS instead of PFHpA in REGULATED_ABBREVS — may need
  mapping when integrating in Step 5. Verify against actual Source Discovery
  report field names before building massdep.py.
- pace_lab_parser.py STREET_EXPANSIONS table is valuable — preserve it during
  Step 5 integration.
- address_lookup.py is NOT needed for scraper integration — map/parcel comes
  from folder path. Operator utility only.

## WATER SERVICE AREA (DEFERRED)
- MassGIS pwsdep_pt/ layer contains well source point locations, NOT service
  area polygons. Not suitable for municipal water soft warning feature.
- check_municipal_water() is stubbed in config.py — assumes no address is on
  municipal water.
- To implement properly: need service area boundary polygon from Wannacomet
  Water Company (operating name for Nantucket Water Works).
  Recommended: email Wannacomet directly requesting a service area shapefile.
- Feature should remain stubbed until proper polygon is obtained.

## NEIGHBORHOOD DATA NOTES
- Final neighborhood list has 23 neighborhoods (Sconset merged into Siasconset).
- 14 features are OSM polygons; 10 were added manually as Points with
  approximate coordinates: Brant Point, Dionis, Madequecham, Miacomet,
  Mid-Island, Nobadeer, Sconset(removed), The Cliff, Tom Nevers, Town.
- Point-based neighborhoods use nearest-point assignment rather than
  polygon containment — less precise. Consider obtaining proper polygon
  boundaries from Town GIS for a future update.
- Tom Nevers coverage was ambiguous in original OSM export — verify assignments
  look correct once Source Discovery records are geocoded.

## SCRAPER NOTES
- Full-island Laserfiche traversal not yet run end-to-end. Only Map 21 tested
  in prototype and Task 4 validation. First full run will be a significant
  operation — monitor closely.
- Laserfiche scraper uses map_filter parameter for targeted testing
  (e.g. map_filter="21"). Useful for debugging specific maps.
- Railway cron entry point: python3 -m app.scrapers.run_laserfiche

## STEP 5 REMINDERS (MassDEP / Source Discovery)
- PFHpS vs PFHpA: RESOLVED. Massachusetts PFAS6 regulatory sum (310 CMR 22.07G)
  uses PFHpA (Perfluoroheptanoic acid, CAS 375-85-9), not PFHpS. Confirmed
  against official MassDEP compound list. Laserfiche scraper is CORRECT using
  PFHpA. Source Discovery prototype parsers (source_discovery_parser.py and
  pace_lab_parser.py) have a bug using PFHpS — corrected in massdep.py to use
  PFHpA. Full PFAS6 list: PFOA, PFOS, PFHxS, PFNA, PFHpA, PFDA.
- pace_lab_parser.py: preserve STREET_EXPANSIONS table
- All 171 Source Discovery records need geocoding — substantial task
- Geocode review queue needed in admin interface for records where
  geocode_review_needed=True
- EEA portal dedup key format: RESOLVED. Changed from URL-based to
  composite key (sample_location, sample_date, medium) in _save_location().
  seen_documents still uses doc_url for PDF-level dedup (prevent re-download).

## MAP / FRONTEND NOTES (added Session 5)
- Address search: geocodes via Nominatim → redirects to /map?lat=&lng=
  &neighborhood=&search_address=. Frontend reads params, flies map via
  fitBounds to include all results within 3km, sorts table by proximity.
  No neighborhood filtering applied — polygon boundaries too imprecise,
  proximity sort is sufficient.
- Results table: proximity sort after search, date-descending on full map
  view. 3km proximity cutoff for table display (markers still show all).
  Retest logic: laserfiche-only, same parcel within operator-configured
  window (default 60 days). Retests flagged with grey "Retest" badge.
  Retest pairs share retest_group_id for row shading. Shading uses
  alternating .retest-group-a / .retest-group-b CSS classes.
- Table rows are clickable — flies map to marker and opens popup. Closes
  any previously open popup first.
- Map scroll behavior: scrollZoom: false always. cooperativeGestures: true
  for iPad/touch (two-finger required). Desktop gets a scroll hint that
  fades after 4 seconds.
- Persistent address search box in controls bar. Stays populated after
  search via search_address param passed through redirect.
- "Get alerts" link in topbar carries ?neighborhood= param when a search
  is active. Signup page pre-selects and scrolls to that neighborhood.
- Mobile number field on signup is commented out pending Twilio setup.
  Look for "SMS DISABLED: Twilio not yet configured" comment in signup.html.
- Disclaimer bar moved to page footer (.disclaimer-footer).
- Time filter options: All time / Last year / Last 6 months / Last 30 days.
- iOS autocorrect note: if a user reports wrong geocode result on iPhone/iPad,
  check whether autocorrect mangled the street name before submission.
  Confirmed Nominatim geocodes Sesachacha Rd correctly from server side —
  likely user/autocorrect error, not a code bug.

## FRONTEND PASS BACKLOG
- In full-island date-sort view, retest pairs can be separated by
  intervening results from other addresses. Consider sorting by group anchor
  date (most recent date in the group) so retest pairs always cluster
  together. Low priority — proximity sort (post-search) handles it correctly
  since pairs have identical distances.
- Retest group shading colors (#edf2f7 / #e2ecf5) should be reviewed for
  accessibility contrast during design pass.
- Overall design/styling pass still ahead — current UI is functional but
  unstyled. Retest badge, status colors, and table layout all TBD.

## EMAIL DELIVERABILITY (post-launch polish)
- Source document links in digest emails point to portal.laserfiche.com and
  MassDEP EEA portal — Resend flags these as mismatched sending domain. Fix:
  route all source document links through ackuifer.org as a redirect proxy
  (e.g. /r?url=https://portal.laserfiche.com/...) so all email links are on
  ackuifer.org. Implement as a simple GET /r route in the app.
- DMARC record missing from DNS. Add TXT record to Squarespace:
  Host: _dmarc
  Value: v=DMARC1; p=none
  (no rua reporting address — do not put any personal email in public DNS)
- Paid tier: users.tier field defaults to 'free' — add 'paid' check before
  gating any features in v2
- Island-wide consolidated digest planned for v2 (currently one email per
  neighborhood)
- Multiple recipients per subscription planned for v2
- SMS for all detections (not just near-MCL) planned for v2
- Stripe integration planned for v2

## ADMIN INTERFACE REMINDERS (Step 8)
- Parse error queue: query seen_documents WHERE parse_status='error'
- Geocode review queue: query source_discovery_results WHERE
  geocode_review_needed=True — operator needs to enter corrected lat/lng
  and mark as resolved
- Editable content panel needed for: municipal water warning text, PFAS info
  page links (section headings, labels, URLs) — must be editable without
  code deploy
- Dead man's switch: separate status indicators for each scraper independently
- Manual trigger buttons for each scraper independently

## DEPLOYMENT STATUS (as of March 17, 2026)
- Live at: https://www.ackuifer.org
- Frontend QA and UX pass complete (Session 5)
- Railway project: ACKuifer (hearty-solace)
- Three Railway services running:
  * Web service — FastAPI app, continuous
  * laserfiche-cron — runs daily at 8 AM UTC (python -m app.scrapers.run_laserfiche)
  * massdep-cron — runs daily at 9 AM UTC (python -m app.scrapers.run_massdep)
- Domain: www.ackuifer.org (CNAME to Railway)
- Root domain ackuifer.org forwards to www.ackuifer.org via Squarespace
- SSL: Auto-provisioned by Railway via Let's Encrypt
- Database: Railway managed PostgreSQL (internal URL in all three services)
- Building footprint privacy fix applied (Mapbox style changed to light-v11)
- DMARC record still needs to be added to Squarespace DNS (see EMAIL DELIVERABILITY)

## NEXT SESSION PRIORITIES
1. Merge claude/objective-hoover to main via PR
2. Resolve remaining 12 geocode-review records: 8 street addresses (Google Maps
   lookups), 4 FRB wells (coordinates from SARSS site map PDF)
3. Run massdep scraper to ingest newer EEA portal PDFs (April, June, August 2025
   data)
4. First full Laserfiche scrape — trigger from admin panel, monitor closely
5. Notification engine — implement alert logic: drinking_water Source Discovery
   alerts same as Laserfiche; groundwater display only; dedup check for
   same-address results across both sources
6. Operator handbook

## HOW TO START NEXT SESSION
Upload CLAUDE.md and BUILD_NOTES.txt to a new chat with Claude.
Open with: "ACKuifer is live. Here are my project files for context.
I want to work on [specific item from NEXT SESSION PRIORITIES]."
